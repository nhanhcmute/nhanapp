const API_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000';

export const couponService = {
  // Lấy coupon theo code (có thể mở rộng sau)
  getCouponByCode: async (code) => {
    // TODO: Implement API endpoint để lấy coupon
    // Hiện tại có thể dùng từ checkout preview
    return null;
  },
};

